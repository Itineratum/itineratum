import { Box } from "@mui/material";
import { useTranslations } from "next-intl";

const Base = () => {
  const t = useTranslations("home.itineraryGenerator.base");

  return <Box></Box>;
};

export default Base;
